apiVersion: v1
kind: Service
metadata:
  name: management-service
  labels: { app: management-service }
spec:
  selector:
    app: management-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080