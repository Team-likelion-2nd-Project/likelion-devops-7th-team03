apiVersion: v1
kind: Service
metadata:
  name: redirect-service
  labels: { app: redirect-service }
spec:
  selector:
    app: redirect-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
